<ResourceDictionary x:Class="w3tools.UI.Templates.DataTemplates"
                    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:vm="clr-namespace:w3tools.App.ViewModels;assembly=w3tools.App"
                    xmlns:v="clr-namespace:w3tools.UI.Views"
                    xmlns:s="clr-namespace:System;assembly=mscorlib"
                    >
    
    <DataTemplate DataType="{x:Type vm:CommandsListViewModel}">
        <v:CommandsView>
            
        </v:CommandsView>
    </DataTemplate>

    <DataTemplate DataType="{x:Type vm:WorkflowListViewModel}">
        <v:WorkflowListView>

        </v:WorkflowListView>
    </DataTemplate>

    <DataTemplate DataType="{x:Type vm:ErrorListViewModel}">
        <v:ErrorsView>

        </v:ErrorsView>
    </DataTemplate>

    <DataTemplate DataType="{x:Type vm:LogViewModel}">
        <v:LogView>

        </v:LogView>
    </DataTemplate>

    <DataTemplate DataType="{x:Type vm:PropertiesViewModel}">
        <v:PropertiesView>

        </v:PropertiesView>
    </DataTemplate>

    <!--
    <DataTemplate DataType="{x:Type vm:VariablesViewModel}">
        <v:VariablesView>

        </v:VariablesView>
    </DataTemplate>
-->

    <DataTemplate DataType="{x:Type vm:DocumentViewModel}">
        <v:DocumentView>

        </v:DocumentView>
    </DataTemplate>



</ResourceDictionary>